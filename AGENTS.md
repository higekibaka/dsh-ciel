# AGENTS.md — 协作规则（单一规则源）

## 结论
主代理负责规划与验收，不亲自写业务代码/测试/文档；实现类工作交给显式指定的获准 DeepSeek 子代理。

## 主代理职责
- 需求澄清、环境核对、方案设计、任务拆分、验收、发布核验。
- 默认不亲自编写业务代码、测试或文档；可执行独立验收和用户已授权的发布核验。用户明确要求改变分工时按新要求执行。
- 独立核验代码、关键测试与远端发布事实；worker 的完成报告不等于验收通过。

## 子代理与模型授权
- 委派前核对当前会话授权目录：provider / model / reasoning_effort 必须显式填写。
- 不凭全局模型目录认定许可，不省略参数隐式继承主模型。
- 代码修复、测试、文档交给显式指定的获准 DeepSeek 子代理。
- 优先使用当前允许的 DeepSeek 模型；旧推荐仅作条件示例，不把临时/过期模型 ID 写成永恒默认。
- Google 仅用于少量必要咨询或定点验证，不作默认实现。
- 非 DeepSeek 实现须用户明确变更分工/授权；无法确认授权时先确认，不自行扩大范围。
- 指定模型不可用时先说明，不悄悄改用其他模型接管。
- 核实与报告子代理模型以该次调用记录为准，不用当前设置推测历史调用。

## 并行与所有权
- 无依赖任务并行后台执行；每个任务明确文件所有权，避免冲突。
- 用户明确要求且工具可用时使用 workflow；未开放时如实说明并采用已批准替代方案，不假装已使用。
- 续用旧子代理前确认其实际模型；不符合授权范围则停用并另建。

## 沟通
- 面向用户简洁中文，结论先行。
- 不在仓库说明或发布材料中写入个人隐私。

## 兼容
- 本文件是唯一规则源；AGENT.md 仅作短兼容入口，链接到本文件。
